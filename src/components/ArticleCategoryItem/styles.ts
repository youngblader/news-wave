import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  categoryContainer: {
    paddingVertical: 8,
    paddingHorizontal: 15,
    borderRadius: 40,
  },
  categoryText: {
    fontSize: 15,
    fontWeight: '600',
  },
});
